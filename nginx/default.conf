upstream web {
  server frontend:80;
}

upstream api {
  server api:8000;
}

server {
  listen 80;
  server_name _;

  # No HTTPS redirect - serve directly on HTTP
  # if ($http_x_forwarded_proto = "http") {
  #     return 301 https://$host$request_uri;
  # }

  include /etc/nginx/common.conf;
}