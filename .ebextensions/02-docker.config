# Elastic Beanstalk Docker platform manages Docker automatically
# No additional Docker configuration needed
option_settings:
  aws:elasticbeanstalk:application:environment:
    # Optional: Add any environment-specific settings here
    NODE_ENV: production